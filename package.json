{
  "name": "axios-stream",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn install && tsc --build tsconfig.json --clean",
    "play": "ts-node src/index.ts"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/winston": "^2.4.4"
  }
}
